#include <iomanip>
#include <string>
#include <iostream>

int main(int ac, char **ag)
{
    if (ac != 2)
    {
        std::cerr << "Arguement two not found" << std::endl;
        return (1);
    }

    return (0);
}